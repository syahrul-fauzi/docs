flowchart LR
  subgraph BC_SecurityCI[Bounded Context: Security/CI]
    GUARD[<<component>> Secret Guard]
    PIPE[<<component>> CI Pipeline]
    IF_SCAN[<<interface>> Scan Results]
  end

  BUILD[<<component>> Build Artifacts]
  BUILD --> GUARD --> IF_SCAN
  PIPE --> GUARD

  subgraph Legend
    C[<<component>> Component]
    IF[<<interface>> Interface/Port]
  end
