---
id: testing.testing.e2e_updates_20251208
version: 1.0.0
author: QA Team
status: active
scope: global
tags: [testing, e2e, updates]
---

# E2E Updates — 2025-12-08

- Base URL: `http://localhost:3001` with `PLAYWRIGHT_SKIP_WEBSERVER=true` for external server usage.
- New API routes:
  - `apps/app/src/app/api/auth/register/route.ts` returns 201 with `message: "Account created successfully"`.
  - `apps/app/src/app/api/auth/forgot-password/route.ts` returns 200 with `message: "Password reset email sent"`.
- UI adjustments:
  - `apps/app/src/app/(public)/register/page.tsx` now posts to `/api/auth/register`, exposes `#alert[role=alert][aria-live=assertive]` and `[role=status]`.
  - `apps/app/src/app/(public)/reset-password/page.tsx` now posts to `/api/auth/forgot-password`, exposes `[role=status][aria-live=polite]`.
- Playwright execution:
  - Targeted grep: "Registration|Reset Password" across Chromium/Firefox/WebKit.
  - Summary generated at `apps/app/playwright-report/run-logs/summary.json`.
  - Validator run with `STRICT_A11Y=false` for this pass.
- Notable failures:
  - Some locators expecting `auth-layout-title` did not resolve consistently; likely environment routing/SSR timing.
  - Registration password requirement assertion conflicted with Next route announcer (`#__next-route-announcer__`).
- Flakiness:
  - Summary computed via `apps/app/scripts/generate-summary-from-playwright.js` — current payload shows 0 due to `.last-run.json` aggregation constraints for grep-only run.
- Next run guidance:
  - Use full suite without grep to collect comprehensive stats and Axe artifacts.
  - Ensure server availability for `PLAYWRIGHT_BASE_URL` and align locale grouping for public pages.
